# Frontend environment variables (loaded by Vite)
# Copy this file to `.env` or `.env.local` and adjust values as needed.
# `.env.local` should NOT be committed.

# Base URL of the backend API that the frontend will call.
# For local dev with Symfony backend running on port 8000:
VITE_API_BASE_URL=http://localhost:8000

# If deploying the frontend under a sub-path, set BASE_URL accordingly.
# BASE_URL=/
